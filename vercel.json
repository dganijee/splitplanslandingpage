{
  "buildCommand": "test -d pocket-ts && (cd pocket-ts && npm install && npm run build) || (npm install && npm run build)",
  "installCommand": "test -d pocket-ts && (cd pocket-ts && npm install) || npm install",
  "framework": "nextjs"
}

